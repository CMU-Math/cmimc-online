These files should ONLY be run inside of the container (except for
cmimc_programming/local_test_code.py), created with a setup as described in the
docker-compose.yml file on a cgroups v2 system.  Running outside of that context
may break things.
